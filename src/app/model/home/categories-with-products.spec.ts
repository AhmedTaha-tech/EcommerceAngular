import { CategoriesWithProducts } from './categories-with-products';

describe('CategoriesWithProducts', () => {
  it('should create an instance', () => {
    expect(new CategoriesWithProducts()).toBeTruthy();
  });
});
